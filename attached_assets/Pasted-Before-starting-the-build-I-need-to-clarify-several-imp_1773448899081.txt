Before starting the build I need to clarify several important rules for this rebuild.

This project is NOT a redesign. It is a faithful rebuild of my existing website using modern technology while preserving the structure, content intent, and SEO.

Important instructions:

1. Source of truth for content
The LIVE WEBSITE is the source of truth for wording, headings, section order, navigation labels, CTA text, and internal linking.

2. PHP files do not contain all content
Much of the website content is controlled through an admin/database system. Because of this, the PHP files will NOT contain all of the visible page wording.

Therefore:
- Do not assume PHP files contain the full content.
- Do not invent or paraphrase missing content.
- If wording is missing from PHP templates, rely on the live rendered page instead.

3. I will guide the rebuild page-by-page
I will provide screenshots of each core page (approximately 35–40 pages). These screenshots must be used as visual reference to preserve the existing layout and section structure.

Use these screenshots to match:
- section order
- headings
- block layout
- spacing hierarchy
- visual grouping

4. No rewriting of content
Do NOT shorten, rewrite, paraphrase, or "improve" the copy. Preserve wording and headings as closely as possible.

5. Brand identity must remain identical
Preserve the existing brand style:

Primary colour: VoiceoverGuy red
Supporting colours: black, white, neutral greys

Do NOT introduce new accent colours or redesign the palette.

6. Preserve URL structure and SEO
The following must remain consistent with the existing site:

- page slugs
- internal linking structure
- navigation hierarchy
- page intent
- section headings where possible

This protects existing SEO.

7. Admin / database driven sections
Some homepage and page sections are dynamically populated (Latest News, blog references, etc).

Build these sections as structured components that can later receive dynamic data, but initially populate them using the content visible on the live site.

8. Technology choice
Build the new site using Next.js (not React + Vite).

9. Workflow
The correct workflow is:

Step 1
Create the site shell (layout, header, navigation, footer, brand styles)

Step 2
Rebuild the homepage faithfully

Step 3
Rebuild the first group of key pages

Step 4
I will then provide screenshots of the remaining pages to guide further builds.

10. If anything is unclear
Ask before inventing or changing content.

Please confirm that these rules are understood and update the rebuild plan before starting the implementation.