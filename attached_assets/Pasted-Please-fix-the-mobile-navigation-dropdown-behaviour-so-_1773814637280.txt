Please fix the mobile navigation dropdown behaviour so parent menu items can toggle open and closed properly on mobile.

PROBLEM
On mobile, the spacing/alignment is now much better, but dropdown menu items do not close when tapped again.
For example:
- tap “Video” once = submenu opens
- tap “Video” again = submenu should close, but it currently stays open

GOAL
Make mobile dropdown parent items behave like proper toggles:
- tap once = open
- tap again = close
- repeat reliably

DESIRED BEHAVIOUR
1. On mobile only, any top-level nav item with a submenu should toggle open/closed when tapped.
2. If a submenu is already open and the user taps the same parent item again, it must close.
3. The open/closed state must update reliably every time.
4. The chevron or indicator should reflect the state clearly if one exists.
5. The interaction should feel clean and predictable on touch devices.

PREFERRED MOBILE BEHAVIOUR
Use accordion-style behaviour on mobile:
- opening one submenu may close the others
- tapping the currently open submenu closes it
This is preferred because it keeps the mobile menu tidy and prevents a giant stack of open panels.

IMPLEMENTATION NOTES
Please inspect the current mobile nav logic and fix the toggle state handling.

Likely issue:
- open state is only being set to “open”
- there is no proper conditional for “if this item is already open, close it”

Please implement logic along these lines:
- if tapped item is not open, set it to open
- if tapped item is already open, set it to closed
- optionally close any other open submenu at the same time for accordion behaviour

If the current code uses separate booleans for each dropdown, simplify if needed.
If the current code uses one active/open key, then make sure tapping the active key again clears it.

TECHNICAL REQUIREMENTS
- Do not break desktop nav hover/dropdown behaviour
- Apply this fix to mobile menu interaction only
- Keep current menu styling
- Keep current spacing improvements
- Keep submenu layout/design as-is unless tiny adjustments are required for proper behaviour
- Make sure tapping the parent item does not unexpectedly navigate away when the intent is to toggle the submenu
- Ensure keyboard/accessibility attributes remain sensible if already present

TEST THESE SPECIFIC ITEMS ON MOBILE
- Voice Demos
- Character Demos
- Video
- Links
- Contact & How Much

SUCCESS RESULT
On mobile:
- tapping a closed dropdown opens it
- tapping the same open dropdown closes it
- submenu behaviour feels deliberate and reliable
- menu no longer gets stuck open

DO NOT
- do not redesign the nav
- do not change desktop behaviour
- do not alter wording
- do not introduce em dashes
- do not add animation clutter unless there is already a subtle transition