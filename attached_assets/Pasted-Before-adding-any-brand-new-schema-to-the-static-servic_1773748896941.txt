Before adding any brand new schema to the static service pages, I want you to investigate whether the old PHP version of VoiceoverGuy already had schema on those pages, because preserving existing working schema is preferable to inventing a completely new setup.

IMPORTANT:
- Do not make changes yet.
- This is an investigation and mapping task first.
- I want continuity where possible.
- If existing schema from the old site was valid and useful, I would rather port that into the new build than replace it with something entirely different.

TASK
Please inspect the old PHP files, shared includes, templates, and any related schema logic from the original VoiceoverGuy site and determine whether schema already existed on the static service pages.

Focus especially on important hand-built pages such as:
- /commercial-voiceover
- /character-voiceover
- /david-attenborough-voice
- /explainer-video-voice
- /voice-of-god
- /game-trailer-voice
- /apple-voice-style
- /football-commentator-voice
- /pirate-voice
- /santa-voice
- /voiceover-studio
- and similar static service pages

FOR EACH PAGE, TELL ME:
1. Did the old PHP version include schema?
2. What schema type(s) did it use?
3. Was the schema defined directly in the page or via a shared include/helper?
4. Is that old schema valid and worth preserving?
5. Has that schema already been migrated into the new build?
6. If not, can the old schema be ported cleanly into the new Next.js page?

I also want you to:
- identify any shared PHP schema logic that applied across multiple pages
- identify whether the old site used Person, Service, LocalBusiness, WebPage, AudioObject, VideoObject, FAQPage, BreadcrumbList, or anything else on these static pages
- flag any old schema that was poor, outdated, invalid, or not worth carrying forward

OUTPUT FORMAT
Please provide:
- Executive summary
- Shared old-PHP schema logic found
- Per-page schema comparison table:
  - URL
  - old schema present? yes/no
  - old schema types
  - already migrated? yes/no
  - recommendation: preserve / recreate / improve / ignore
- Final recommendation on whether we should:
  A. port the old schema as-is
  B. port and lightly modernise it
  C. create fresh schema only where nothing useful existed before

IMPORTANT
- Truth over convenience
- Do not assume missing schema in the new build means none existed before
- I want to protect SEO continuity, not just create something new because it is easier